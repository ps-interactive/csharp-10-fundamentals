rules:
  - id: untitled_rule
    patterns:
      - pattern: |
            if ($A == 18) { 
                Console.WriteLine("You are eligible");
                Console.WriteLine("Congratulations");
            }
      - pattern: |
            if (18 == $A) { 
                Console.WriteLine("You are eligible");
                Console.WriteLine("Congratulations");
            }
    message: <pass>
    languages: [csharp]
    severity: WARNING