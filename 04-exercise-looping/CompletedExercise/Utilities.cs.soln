namespace Looping
{
    public class Utilities
    {
        public static int CalculateSum (int startValue)
        {
            int sum = 0;
            for (int i = startValue; i > 0; i--)
            {
                sum += i;

            }
            return sum;
        }
    }
}
